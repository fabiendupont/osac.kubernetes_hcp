name: osac-kubernetes-hcp
display_name: Kubernetes (Hosted Control Planes)
description: Kubernetes cluster provisioning via Hosted Control Planes for OSAC
version: 0.1.0

dependencies:
  osac_core: ">=0.1.0"

resource_types:
  - name: Cluster
    scope: tenant
