resource_type: ClusterOrder
event: Delete
phase: main
priority: 100
failure_policy: Fail

title: HCP Kubernetes Cluster with GitHub Integration Deletion
description: >
  Deprovisions an OpenShift cluster with GitHub integration.
  Delegates to cluster.delete.main.

parameters:
  - name: cluster_order
    type: dict
    required: true
    description: ClusterOrder resource to deprovision

outputs: []
